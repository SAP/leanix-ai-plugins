/**
 * Hello World Calculation
 *
 * Fact Sheet Type: Application
 * Target Field: calculatedLabel (String)
 * Logic: Returns a static value - simplest possible calculation
 *
 * Use this to verify calculation setup is working.
 */

export function main() {
  return "Calculated";
}
